
services:
# Redis Cache
  redis:
    image: redis:latest
    container_name: redis_cache
    command: redis-server --requirepass redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    restart: always

volumes:
  redis_data:


